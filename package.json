{
  "name": "webvalia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --source ./dist",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^4.12.0",
    "pagefind": "^1.1.0"
  }
}

